interface ISelectEditing {
    id?: string
    name?: string
}

export const SelectEditor = ({ id, name }: ISelectEditing) => {

    return (
        <div>
            SelectEditing
        </div>
    );
};
