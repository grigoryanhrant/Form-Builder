interface ISelectEditing {
    id?: string
    name?: string
}

export const SelectEditing = ({ id, name }: ISelectEditing) => {

    return (
        <div>
            SelectEditing
        </div>
    );
};
