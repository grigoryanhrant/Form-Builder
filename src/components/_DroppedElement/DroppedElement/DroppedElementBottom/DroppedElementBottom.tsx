import React from 'react';
import {IDroppedElement} from "./types/types";

export const DroppedElementBottom = ({id, type, name, placeholder, value}: IDroppedElement) => {
    return (
        <div>

        </div>
    );
};
